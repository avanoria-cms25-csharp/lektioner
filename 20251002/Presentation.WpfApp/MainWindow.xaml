<Window x:Class="Presentation.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation.WpfApp"
        
        xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
        xmlns:views="clr-namespace:Presentation.WpfApp.Views"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        
        Background="SlateGray"
        WindowStyle="None"
        ResizeMode="NoResize"
        
        Title="{Binding Title, FallbackValue=FAN}" Height="450" Width="300">


    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Foreground="White" Padding="10" Grid.Column="0" Text="{Binding Title, FallbackValue=FAN}" />
                <Button Style="{StaticResource btn-close}" Grid.Column="1" x:Name="BtnCloseApp" Click="BtnCloseApp_Click"   />
            </Grid>
            
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
                        <views:HomeView />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>

        </Grid>
    </Border>
    
</Window>
