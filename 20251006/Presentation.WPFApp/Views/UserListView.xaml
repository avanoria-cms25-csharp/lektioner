<UserControl x:Class="Presentation.WPFApp.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPFApp.Views"
             
             xmlns:viewmodels="clr-namespace:Presentation.WPFApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:UserListViewModel}"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <StackPanel>
            <TextBlock Text="{Binding Title}" />
            <Button Command="{Binding GoToUserAddViewCommand}" Content="NEW USER" />
        </StackPanel>

    </Grid>
</UserControl>
